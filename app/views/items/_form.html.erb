<%= form_with(model: item, local: true) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


   <div class="field">
    <%= form.label :team_id %>
    <%= form.number_field :team_id, id: :item_team_id %>
  </div>

   <div class="field">
    <%= form.label :filestack_url %>
    <%= form.filestack_field :filestack_url, 'Upload file', id: :item_filestack_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
